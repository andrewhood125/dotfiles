google() {
  # IFS is a special var which sets the delimeter
  # in conjunction with $*
  # http://tldp.org/LDP/abs/html/internalvariables.html#IFSREF
  IFS="+"
  open "https://google.com/search?q=$*"
}

vimrc() {
  vim ~/.vimrc
}

zshrc() {
  vim ~/.zshrc
  reload
}

sugar() {
  vim ~/.sugar
  reload
}

reload() {
  source ~/.zshrc
}

alias tags='git tag --list | gsort --version-sort'
alias :q='exit'

# vim: set ft=sh :
